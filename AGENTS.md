# Контекст проекта: Интерактивный ИИ-робот Мелвин (Melvin)

## 🤖 Обзор проекта
Мелвин — это автономный голосовой ассистент на базе ESP32-S3 (плата SpotPear ESP32-S3-1.54 V2.0). 
Он использует локальный VAD (Voice Activity Detection) для прослушивания эфира, записывает речь, отправляет её в облако (Gemini/Groq) для "обдумывания", генерирует ответ через TTS и воспроизводит его через профессиональный аудио-тракт.

## ⚙️ Железо и Распиновка (SpotPear V2.0)
Агенты ДОЛЖНЫ учитывать эти пины при генерации кода. Изменение распиновки сломает устройство.

| Компонент | Интерфейс | GPIO Пины | Примечания |
|---|---|---|---|
| **ES8311 (Аудиокодек)** | I2C | SDA=15, SCL=14 | Адрес по умолчанию |
| **ES8311 (Аудиокодек)** | I2S | MCLK=16, BCLK=9, WS=45, DOUT=8, DIN=10 | MCLK критичен для старта |
| **Усилитель (NS4150B)** | GPIO | PA_CTRL=46 | **Внимание:** Strapping pin! |
| **SD Карта** | SDMMC (1-bit) | CLK=17, CMD=18, D0=21 | Только 1-bit режим |
| **Дисплей (ST7789)** | SPI | SCLK=4, MOSI=2, DC=47, CS=5, RST=38, BL=42 | 240x240 |
| **Кнопка BOOT** | GPIO | GPIO0 | Используется для ручного триггера |

## ⚠️ Критические аппаратные ограничения (ОБЯЗАТЕЛЬНО К ПРОЧТЕНИЮ)

1. **PA_CTRL (GPIO46) - Усилитель:** 
   - Это strapping pin. При загрузке (boot) он ДОЛЖЕН быть в состоянии `LOW`.
   - Включать (`HIGH`) только непосредственно перед отправкой данных в I2S TX.
   - Выключать (`LOW`) сразу после окончания воспроизведения, чтобы избежать белого шума и сэкономить батарею.
2. **MCLK (GPIO16):** 
   - Тактирование I2S (MCLK) должно быть стабилизировано ПЕРЕД инициализацией I2C регистра ES8311. Иначе кодек не заведется.
3. **Переключение I2S (RX <-> TX):**
   - ES8311 и драйвер ESP32 не любят резких смен направления. 
   - Алгоритм: `i2s_channel_disable(RX)` -> `delay(10)` -> `i2s_channel_enable(TX)` (и наоборот).
4. **SD Карта:** Работает в 1-bit режиме, чтобы не конфликтовать с пинами дисплея и I2S. Формат строго FAT32.

## 🎵 Аудио-стандарт
Все операции с аудио (запись с микрофона MB23 H11W, отправка в API, получение из TTS, чтение с SD) должны соответствовать стандарту:
- **Формат:** WAV (PCM) / Raw PCM
- **Частота:** 16000 Гц (16kHz)
- **Битность:** 16 бит (int16_t)
- **Каналы:** Моно (1 канал)
*Любое отклонение приведет к тому, что VAD не сработает, а Gemini/TTS вернет ошибку или "роботизированный" звук.*

## 🏗 Архитектура ПО (Dual Framework)
В проекте исторически присутствуют два пути. Агенты должны уточнять, какой файл мы сейчас редактируем:
1. **Arduino / PlatformIO (`src/main.cpp`)**: Использует библиотеку `earlephilhower/ESP32-audioI2S`. Подходит для быстрых тестов, но может вызывать конфликты MCLK.
2. **ESP-IDF (`src/main_idf.c`)**: Использует "сырой" `driver/i2s_std.h`. **Это Production-стандарт** для стабильной работы ES8311 без зависаний.
*Правило:* При написании сложных DSP-алгоритмов или работе с DMA-буферами приоритет отдается подходу из `main_idf.c`.

## 🧠 AI Pipeline и Память
- **Память:** ESP32-S3 имеет PSRAM. Буферы записи аудио (до 10 сек) и Base64-декодирование TTS должны аллоцироваться в PSRAM (`heap_caps_malloc(size, MALLOC_CAP_SPIRAM)`), чтобы не убить внутреннюю RAM.
- **Контекст:** История диалогов хранится в `/history.json` на SD. Перед запросом к LLM агент должен прочитать этот файл и добавить последние N реплик в системный промпт.
- **Каскад API:** Если Gemini 3.5 Flash недоступен, код должен автоматически переключаться на Groq или OpenRouter.

## 🚫 Запрещенные действия для AI-агентов
- НЕ использовать `delay()` внутри аудиостриминга или I2S callback-функций (использовать FreeRTOS tasks и очереди).
- НЕ аллоцировать память (`malloc`/`new`) внутри ISR (прерываний).
- НЕ игнорировать проверку результата `i2s_channel_write` (если буфер переполнен, нужно делать micro-delay).
- НЕ менять частоту семплирования на 44100/48000 Гц без крайней необходимости (VAD и API настроены на 16кГц).